version: 3

services:
  # web:
  #   image: nginx:latest
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./html:/usr/share/nginx/html
  #   networks:
  #     - webnet

  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: roomie_buddy
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - roomie-buddy
    ports:
      - "3306:3306"
      # mysql://root:root@db:3306/roomie_buddy